apiVersion: v1
kind: Service
metadata:
  labels:
    app: zackward-dot-net
  name: zackward-dot-net
spec:
  ports:
  - port: 80
    name: http
    protocol: TCP
  - port: 443
    name: https
    protocol: TCP
  selector:
    app: zackward-dot-net
